<UserControl x:Class="Client.Views.MessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
                  xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
         
        </Grid.RowDefinitions>

        <fa:ImageAwesome Name="ImageAwesome"  Icon="ClockOutline" Margin="100"  Width="80" Height="80" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button Content="Ok" Grid.Row="1"  Grid.Column="0"  Width="70"   Margin="10" Height="50" Command="{Binding BackCommand}"></Button>
    </Grid>
</UserControl>
